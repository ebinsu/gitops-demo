apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: traefik
  namespace: traefik
spec:
  interval: 15m
  timeout: 5m
  chart:
    spec:
      chart: traefik
      version: '38.0.2'
      sourceRef:
        kind: HelmRepository
        name: traefik
      interval: 5m
  releaseName: traefik
  values:
    experimental:
      kubernetesGateway:
        enabled: true
    ingressClass:
      enabled: false
    providers:
      kubernetesIngress:
        enabled: false
      kubernetesGateway:
        enabled: true
        experimentalChannel: true
    gateway:
      listeners:
        web:
          namespacePolicy:
            from: All
        team-a-session:
          namespacePolicy:
            from: All
          port: 16379
          protocol: TCP
    ports:
      team-a-session:
        port: 16379
        expose:
          default: true
        exposedPort: 16379
        protocol: TCP